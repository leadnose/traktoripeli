// Entry point. Everything not yet split into its own module still lives in
// legacy.js — imported here purely for its side effects (it wires up input
// listeners, generates the map and starts the main loop on its own).
import "./legacy.js";
